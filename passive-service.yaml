server:
  port: 8086
eureka:
  client:
    region: default
    service-url:
      default-zone: http://localhost:8082/eureka/
    registry-fetch-interval-seconds: 5
spring:
  application:
    name: passive-service
  

